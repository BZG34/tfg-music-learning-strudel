server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Si no encuentra el archivo, carga index.html
        try_files $uri $uri/ /index.html; 
    }
}